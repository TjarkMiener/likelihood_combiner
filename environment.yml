# conda env create -f environment.yml
name: lklcom
channels:
    - conda-forge
    - menpo
dependencies:
    - jupyter
    - matplotlib
    - numpy>=1.15.0
    - scipy
    - pandas
    - pyyaml
    - pytables
